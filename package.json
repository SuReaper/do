{
  "name": "poc",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker deployment template",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}